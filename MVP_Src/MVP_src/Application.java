package MVP_src;

public class Application{

    /**
     * Constructor that instantiates the GUI at a pre-determined percentage of screen size
     */
    public Application(){
        new GUI(90);
    }
}
